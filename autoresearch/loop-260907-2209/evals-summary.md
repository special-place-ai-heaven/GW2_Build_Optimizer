# Bounded experiment outcome: BLOCKED

Three hypotheses were evaluated. Direct plating alone regressed strict validity
from 3/6 to 1/6 and was reverted. Exact prefix evidence plus direct plating reached
5/6 and remains a provisional local candidate. Prefix-only measurements were
interrupted after four completed attempts, so no six-run aggregate is claimed.

Current OpenRouter holdouts passed: Scourge 100.7 seconds / 10 HTTP attempts;
Ritualist 25.9 seconds / 6. Native Gemini holdouts failed at 231.3 and 314.9 seconds.
The original quota diagnosis does not explain those later failures by itself.

The TSV median includes failed attempts and cannot establish faster valid builds.
Small samples span days and provider conditions. The pinned verifier remains
PENDING because baseline failures violate its all-pass prerequisite; one candidate
and two native holdouts also fail. No retries replace failed rows. Further edits
cannot repair the historical baseline prerequisite, so the run checkpoints BLOCKED.

Guard: 1112 passed, 0 failed, 4 ignored; example build, strict Clippy and formatting
passed. No in-game/referee validation, release build, push or deployment occurred.
See `docs/choya-latency-review.md` and open task LAT-04 for the follow-up.
