# Tasks: Audit remediation

User override (2026-09-05): work in small sprints and commit often. Verified local sprint commits may precede in-game acceptance, superseding wait-before-commit wording below. Stage only owned changes; another agent is actively modifying this shared tree. No push/release is implied.

Input: spec.md, plan.md, research.md, data-model.md, contracts/remediation.md. Detailed claims and acceptance: ledger.md. All tasks preserve existing user/other-agent edits.

## Phase 1: Setup

- [X] T001 Capture baseline and import all report IDs in specs/003-audit-remediation/ledger.md.

## Phase 2: Foundation

- [X] T002 Establish risk order, persistence/stat contracts and verification gates in specs/003-audit-remediation/plan.md and contracts/remediation.md.

## Phase 3: US1 — Preserve player data and host stability (P1)

Independent acceptance: see spec.md US1; each item must satisfy its own ledger acceptance and dependencies. Write behavioral regression tests before the corresponding fix. Exact target is the source path below; use SymForge to resolve current symbols rather than historical line numbers.

- [ ] T003 [US1] W011: Return a fallible history load; keep a session write-refusal on addon feedback state after any non-NotFound failure, surface the error, and propagate refusal into flush_dirty. Target: `crates/core/src/feedback/store.rs`. Record evidence in ledger.md#w011.
- [ ] T004 [US1] W006: Extract the Lock All spec/trait mutation into a helper that iterates only locks.specs.len() entries; preserve the existing bounded trait-column lookup. Target: `crates/addon/src/ui/main_view/lock_panel.rs`. Record evidence in ledger.md#w006.
- [ ] T005 [US1] W002: Share URL host normalization and reserved-address screening between radio player and logos; test bracketed loopback, mapped IPv4, link-local and public literals. Target: `crates/addon/src/radio/player.rs`. Record evidence in ledger.md#w002.
- [ ] T006 [US1] W012: Use crate::storage::replace_file for atomic feedback and taxonomy publication, preserving temporary-file cleanup and the previous file on errors. Target: `crates/core/src/feedback/store.rs`. Depends on W011. Record evidence in ledger.md#w012.
- [ ] T007 [US1] W001: Fix all ten observed Clippy diagnostics without relaxing CI; verify workspace Clippy including all targets. Target: `.github/workflows/ci.yml`. Record evidence in ledger.md#w001.

## Phase 4: US2 — Trust builds and calculations (P2)

Independent acceptance: see spec.md US2; each item must satisfy its own ledger acceptance and dependencies. Write behavioral regression tests before the corresponding fix. Exact target is the source path below; use SymForge to resolve current symbols rather than historical line numbers.

- [ ] T008 [US2] B001: Pass the accepted ValidatedBuild into attach_chat_stats and use engine::calculate_validated_stats with balance context and returned modifiers. Target: `crates/addon/src/ui/main_view/optimization.rs`. Record evidence in ledger.md#b001.
- [ ] T009 [US2] W025: Return an explicit tool error when prefix/stat resolution fails; never invent a stat block. Target: `crates/optimizer/src/gemini_tools.rs`. Record evidence in ledger.md#w025.
- [ ] T010 [US2] W035: Build SimParams from resolved stats and balance mode in the tool and call simulate_with; restrict the basic test convenience to actual test consumers. Target: `crates/optimizer/src/rotation/simulator.rs`. Depends on W025. Record evidence in ledger.md#w035.
- [ ] T011 [US2] W019: Implement profile viability requirements in the referee with failed/pass boundary tests; preserve mode-specific semantics. Target: `crates/optimizer/src/data/objective_profiles.rs`. Depends on W044. Record evidence in ledger.md#w019.
- [ ] T012 [US2] W044: Make normalized-effect patch mismatch explicit in production and active-manifest consistency validation; historical data must not be silently relabeled as current verified evidence. Target: `data/normalized_effects/2026-01-13/pve.json`. Record evidence in ledger.md#w044.
- [ ] T013 [US2] W013: Derive the active patch from manifests, retain explicit historical lookup, and make unsupported live-build/patch mismatch observable. Target: `crates/optimizer/src/balance.rs`. Depends on W044. Record evidence in ledger.md#w013.
- [ ] T014 [US2] W015: Wire manifest staleness detection to the live build-number result and visible data status. Target: `crates/optimizer/src/data/manifests.rs`. Depends on W013. Record evidence in ledger.md#w015.
- [ ] T015 [US2] W016: Wire data initialization to startup and propagate disabled/degraded status; no success-quality fallback after a loader failure. Target: `crates/optimizer/src/data/mod.rs`. Depends on W044. Record evidence in ledger.md#w016.
- [ ] T016 [US2] W039: Count unsupported proc categories and zero-duration unsupported healing in coverage; test reporting without counting the same source repeatedly per tick. Target: `crates/optimizer/src/rotation/wvw_timeline.rs`. Record evidence in ledger.md#w039.
- [ ] T017 [US2] W038: Replace both literals with `crate::data::boon_condition_formulas::boons().protection_multiplier()`, matching simulator.rs:653. Target: `crates/optimizer/src/rotation/wvw_timeline.rs`. Record evidence in ledger.md#w038.
- [ ] T018 [US2] W008: Give `BuildSuggestion` typed fields (`heal: Option<SkillRef>`, `utilities: [Option<SkillRef>; 3]`, `elite`, `pets`, `stances`) and format the display strings from those at render time; delete all three parsers. Target: `crates/addon/src/ui/main_view/optimization.rs`. Record evidence in ledger.md#w008.
- [ ] T019 [US2] W004: Route these through `if let Err(e) = ... { crate::ui::log_disk_error(format!("config save failed: {e}")) }`, matching `save_config_detached`. Target: `crates/addon/src/state.rs`. Record evidence in ledger.md#w004.
- [ ] T020 [US2] W009: Replace every `let _ = state.config.save(&state.config_path)` in settings.rs and news.rs with `crate::ui::save_config_detached(state)` (already used in the theme section of the same file); if a synchronous save is genuinely required somewhere, `if let Err(e) = .. { nexus::log::log(Warning, ..) }` as settings.rs:238 already does. Target: `crates/addon/src/ui/main_view/tabs/settings.rs`. Record evidence in ledger.md#w009.
- [ ] T021 [US2] W082: Check the second spawn's result and, on `false`, either write the snapshot synchronously (it is a tiny JSON) or set `state.main.error` to say the notes were not saved; alternatively revert the in-memory notes mutation. Target: `crates/addon/src/ui/main_view/tabs/saveload.rs`. Record evidence in ledger.md#w082.
- [ ] T022 [US2] W182: Drop the `is_empty() ||` clause and give the diag fixture palette entries (as the sibling test already does), so the seed and the beam apply one gating rule. Target: `crates/optimizer/src/synergy_pipeline.rs`. Record evidence in ledger.md#w182.
- [ ] T023 [US2] W225: On parse failure push a tool result of {"error":"unparseable arguments: <err>"} (so the model can retry) and count/report it the way sse.rs reports skipped payloads, instead of executing with empty args. Target: `crates/optimizer/src/llm/openai.rs`. Record evidence in ledger.md#w225.
- [ ] T024 [US2] W157: Use `saturating_add` for every `now_ms`/`current_time_ms` + duration expression, matching the sibling lines that already do. Target: `crates/optimizer/src/rotation/wvw_timeline.rs`. Record evidence in ledger.md#w157.
- [ ] T025 [US2] W231: `u16::try_from(code).unwrap_or(502)` so an out-of-range code falls back to the generic 502 instead of wrapping. Target: `crates/optimizer/src/llm/sse.rs`. Record evidence in ledger.md#w231.
- [ ] T026 [US2] W034: Compute only the scenario-selected combat tier during referee candidate evaluation and remove unused report fields after caller/fixture updates. Target: `crates/optimizer/src/referee.rs`. Record evidence in ledger.md#w034.

## Phase 5: US3 — Consolidate critical policies (P3)

Independent acceptance: see spec.md US3; each item must satisfy its own ledger acceptance and dependencies. Write behavioral regression tests before the corresponding fix. Exact target is the source path below; use SymForge to resolve current symbols rather than historical line numbers.

- [ ] T027 [US3] W003: Delete the two private copies in ui/main_view/tabs/radio.rs and import the already-`pub` `player::saved_from_station` / `player::station_from_saved` (the file already does `use crate::radio::{..., player, ...}`); keep one round-trip test. Target: `crates/addon/src/radio/player.rs`. Record evidence in ledger.md#w003.
- [ ] T028 [US3] W005: Collapse to one renderer parameterised by a small `SheetSide { primary_prefix, other_prefix, primary_name }` resolver (or a closure pair), so the armor/trinket/relic/weapon loops exist once. Target: `crates/addon/src/ui/gear_sheet.rs`. Record evidence in ledger.md#w005.
- [ ] T029 [US3] W007: Extract one `paint_spec_row(ui, geometry, spec, trait_state, style)` that both call, with an enum or a small `interactive: bool` for the click/tooltip handling, and hoist the sizing block into a single `LockGeometry::for_width(ui, avail)`. Target: `crates/addon/src/ui/main_view/lock_panel.rs`. Record evidence in ledger.md#w007.
- [ ] T030 [US3] W010: Delete the `legacy` branch here and the `LEGACY_FIRST_WINDOW_SIZE` const, or move the one-shot upgrade into `AppConfig::load` gated on a stored config schema version so it can only fire once per install. Target: `crates/addon/src/ui/mod.rs`. Record evidence in ledger.md#w010.
- [ ] T031 [US3] W014: Remove the unused no-op check_wvw_quality and its re-export, including the unreachable placeholder lookup (W106), after proving current callers are absent. Target: `crates/optimizer/src/data/balance_overrides.rs`. Record evidence in ledger.md#w014.
- [ ] T032 [US3] W017: Keep the live synergy scorer and remove the unused parallel score_effect implementation and exclusive helpers/re-export after reference checks. Target: `crates/optimizer/src/data/normalized_effects.rs`. Record evidence in ledger.md#w017.
- [ ] T033 [US3] W018: Remove unused legacy-effect mapper and exclusive helpers/re-export after reference checks; retain any still-live synergy import. Target: `crates/optimizer/src/data/normalized_effects.rs`. Record evidence in ledger.md#w018.
- [ ] T034 [US3] W020: Retain patch ledgers as test-time provenance: gate the module appropriately and move serde_yaml to dev dependencies if no runtime consumer remains. Target: `crates/optimizer/src/data/patch_ledger.rs`. Depends on W013. Record evidence in ledger.md#w020.
- [ ] T035 [US3] W021: Wire supported rotation profile controls to their intended calculations; validate objective profile references and reject unsupported controls explicitly instead of silently ignoring them. Target: `crates/optimizer/src/data/rotation_profiles.rs`. Depends on W019. Record evidence in ledger.md#w021.
- [ ] T036 [US3] W022: Remove unused production wrappers; port affected tests to cancellable entry points or retain an explicitly test-only convenience. Target: `crates/optimizer/src/engine.rs`. Record evidence in ledger.md#w022.
- [ ] T037 [US3] W023: Delete the orphaned blocks at engine.rs:1103-1116, engine.rs:1765-1767, engine.rs:2278-2284 and validation.rs:430-433, keeping only the doc line that actually describes the following item; move `validate_gemini_build`'s doc down to validation.rs:499; collapse the duplicated paragraph at validation.rs:304-307 vs 312-318 into one; and fix 'silently logged' to state what the code does (see the swallowed-error finding). Target: `crates/optimizer/src/engine.rs`. Record evidence in ledger.md#w023.
- [ ] T038 [US3] W024: Remove unused buff indexes, population/dedup work and accessors once fresh reference queries confirm no consumers. Target: `crates/optimizer/src/gamedb.rs`. Record evidence in ledger.md#w024.
- [ ] T039 [US3] W026: Extract one tokenizer (markup strip + number/percent/stat-name extraction) into a shared module and have all three call sites map its output into their own result type, rather than each re-scanning the raw string. Target: `crates/optimizer/src/gemini_tools.rs`. Depends on W032. Record evidence in ledger.md#w026.
- [ ] T040 [US3] W027: Extract shared retry policy using provider-specific request/reader closures; retain intentional per-provider retry limits with documentation and transport regression tests. Target: `crates/optimizer/src/llm/anthropic.rs`. Depends on W030. Record evidence in ledger.md#w027.
- [ ] T041 [US3] W028: Remove unused response-cache trait methods and provider state after current reference analysis; update live tests to exercise shipping generate methods. Target: `crates/optimizer/src/llm/mod.rs`. Depends on W030. Record evidence in ledger.md#w028.
- [ ] T042 [US3] W029: Unify quota display with the persisted usage reader that Settings actually uses; remove uncalled quota trait methods and misleading UI claims without discarding rate-limit enforcement. Target: `crates/optimizer/src/llm/mod.rs`. Depends on W030. Record evidence in ledger.md#w029.
- [ ] T043 [US3] W030: Consolidate the OpenAI-compatible tool loop and shared wrapper policy in openai_compat; preserve provider URLs, headers, reasoning settings and model filters. Target: `crates/optimizer/src/llm/openai.rs`. Record evidence in ledger.md#w030.
- [ ] T044 [US3] W031: Consolidate with W030; verify this entry's distinct claim before closing. Target: `crates/optimizer/src/llm/openai.rs`. Depends on W030. Record evidence in ledger.md#w031.
- [ ] T045 [US3] W032: Extract a production Fact classification core consumed by combat and synergy, preserving independent output projection tests across handled Fact variants. Target: `crates/optimizer/src/parser_consistency_tests.rs`. Record evidence in ledger.md#w032.
- [ ] T046 [US3] W033: Remove unused build_game_context and its private tests after moving any still-required mode discipline into the live prompt builders. Target: `crates/optimizer/src/prompts.rs`. Record evidence in ledger.md#w033.
- [ ] T047 [US3] W036: Consolidate sourced activation timings in balance override data while preserving explicit aftercast semantics and fallback evidence; test overlapping skills before removing the table. Target: `crates/optimizer/src/rotation/skill_timings.rs`. Record evidence in ledger.md#w036.
- [ ] T048 [US3] W037: Remove unwritten identity fields only after tracing passive parameter folding and preserving coverage accounting in W039. Target: `crates/optimizer/src/rotation/wvw_timeline.rs`. Depends on W039. Record evidence in ledger.md#w037.
- [ ] T049 [US3] W040: Remove the uncalled ObjectiveScorer twin after reference checks; preserve live calibrated score_with_weights constants and regression expectations. Target: `crates/optimizer/src/scoring.rs`. Record evidence in ledger.md#w040.
- [ ] T050 [US3] W041: Delete the local `prefixes` array and `padded_alnum_words`, make `scoring::prefix_named_in_text` public to the crate (it is already `pub`), and call it: `extract_gear_prefix(html)` becomes `prefix_named_in_text(html).unwrap_or_default().to_string()`. Target: `crates/optimizer/src/scraper.rs`. Record evidence in ledger.md#w041.
- [ ] T051 [US3] W042: Remove unconstructible legacy effect variants and exclusive rules after normalized mapper cleanup; preserve live normalized proc support. Target: `crates/optimizer/src/synergy.rs`. Record evidence in ledger.md#w042.
- [ ] T052 [US3] W043: Either delete `extract_percent_before` and its 6 tests outright, or drop the `#[cfg(test)]` gate and make combat.rs/synergy.rs actually call it — then the comment becomes true. Target: `crates/optimizer/src/text_util.rs`. Depends on W032. Record evidence in ledger.md#w043.
- [ ] T053 [US3] W045: Re-derive the tier list from the actual call sites in crates/addon/src/ui/main_view/optimize_flow.rs and update both docs/architecture.md:20-22 and CLAUDE.md:30/37-41 with the real entry points (optimize_deterministic_cancellable, optimize_cancellable) and whatever the LLM tier is called now. Target: `docs/architecture.md`. Record evidence in ledger.md#w045.

## Phase 6: US4 — Resolve remaining S3 debt (P4)

Independent acceptance: see spec.md US4; each item must satisfy its own ledger acceptance and dependencies. Write behavioral regression tests before the corresponding fix. Exact target is the source path below; use SymForge to resolve current symbols rather than historical line numbers.

- [ ] T054 [US4] W046: Change the rule in .gitignore to un-anchored `.symforge/` (matching the `.git/info/exclude` pattern) so every clone ignores the nested directories. Target: `.gitignore`. Record evidence in ledger.md#w046.
- [ ] T055 [US4] W047: Rewrite the doc to match the code: BOOTSTRAP_FAILED latches a panicked attach so the PostRender bootstrapper stops retrying for the session (a retry loop would re-panic every frame); recovery requires an addon reload. Target: `crates/addon/src/lib.rs`. Record evidence in ledger.md#w047.
- [ ] T056 [US4] W048: Return the failure instead of dropping it: make `fetch_body` return `Result<String, String>` (the shape `radio::directory::fetch` already uses at directory.rs:252) and have `kick` log the reason once per failed source via `nexus::log::log(LogLevel::Warning, ...)`, optionally storing it in a per-source error slot so the empty state can say why. Target: `crates/addon/src/news.rs`. Record evidence in ledger.md#w048.
- [ ] T057 [US4] W049: Reuse the logos discipline in news_art: a `CREATED` set gated by a `MAX_TEXTURES` cap before `get_texture_or_create_from_file`, and a call to a shared `evict_oldest(dir, MAX_CACHE_FILES)` after the tmp+rename write in `download`. `evict_victims` in logos.rs:388 is already pure and unit-tested — lift it to a shared helper rather than copying it. Target: `crates/addon/src/news_art.rs`. Record evidence in ledger.md#w049.
- [ ] T058 [US4] W050: Reword the module doc to say the sprite blits are clipped to the bar rect while the quip bubble deliberately renders on the foreground draw list above everything, and note the `right_limit` clamp at art.rs:576-579 as the actual mechanism that keeps the bubble off the hearts column. Target: `crates/addon/src/radio/art.rs`. Record evidence in ledger.md#w050.
- [ ] T059 [US4] W051: Rewrite the `draw_quip_bubble` doc to describe the five above-the-head anchors actually implemented, inline `alpha` and delete the vestigial `let a = alpha;` binding, and drop `EQ_WARM` from the art.rs:23 rect-table note. Target: `crates/addon/src/radio/art.rs`. Record evidence in ledger.md#w051.
- [ ] T060 [US4] W052: Move lines 1069-1071 down to sit directly above `fn finish_stopped` at player.rs:1094, leaving lines 1072-1076 as `stream_host_reserved`'s doc. Target: `crates/addon/src/radio/player.rs`. Record evidence in ledger.md#w052.
- [ ] T061 [US4] W053: Delete the field and its two write sites in stats.rs, or wire it into the localized-names guard if that was the intent (stats.rs:163 sets it right where such a guard would belong). Target: `crates/addon/src/state.rs`. Record evidence in ledger.md#w053.
- [ ] T062 [US4] W054: Drop the redundant `config.has_gw2_key() &&`, and replace the dead `else` arm by making the `has_gw2_key()` branch the `else` (or leave one arm and delete the other). Target: `crates/addon/src/state.rs`. Record evidence in ledger.md#w054.
- [ ] T063 [US4] W055: Normalise once at load (`SavedBuild` deserialization) using a stored schema/version field rather than sniffing, then delete this function and have the renderer just multiply by 100. Target: `crates/addon/src/ui/comparison.rs`. Depends on W083. Record evidence in ledger.md#w055.
- [ ] T064 [US4] W056: Delete the line. Target: `crates/addon/src/ui/comparison.rs`. Record evidence in ledger.md#w056.
- [ ] T065 [US4] W057: Hoist a single private `fn strip_label_ci` to module scope and call it from both parsers. Target: `crates/addon/src/ui/gear_diff.rs`. Depends on W008. Record evidence in ledger.md#w057.
- [ ] T066 [US4] W058: Add `"Relic" => "slot.relic"` to `slot_label`'s match and pass `slot_label("Relic")` at both call sites (note `row` takes `slot: impl AsRef<str>`, so `&String` works). Target: `crates/addon/src/ui/gear_sheet.rs`. Record evidence in ledger.md#w058.
- [ ] T067 [US4] W059: Add `err.no_api_key`, `err.refresh_failed`, `err.gamedb_load`, `err.cache_write` and `status.refreshing` to the catalogue and route these through `t`/`tf`, keeping the underlying error text as the interpolated detail. Target: `crates/addon/src/ui/main_view/character.rs`. Record evidence in ledger.md#w059.
- [ ] T068 [US4] W060: Measure the widest localized name actually being rendered this frame (the spec names already resolved for the three slots), or take a `&str` parameter and pass the longest of them. Target: `crates/addon/src/ui/main_view/lock_panel.rs`. Record evidence in ledger.md#w060.
- [ ] T069 [US4] W061: Replace the literals with `theme::OPTIMIZED` / `theme::pal()` entries, adding palette slots for the panel fill and separator if none fit; do the same for `SELECTED_COLOR` / `DIM_COLOR` / `AVAILABLE_COLOR` / `ELITE_COLOR` at 12-15. Target: `crates/addon/src/ui/main_view/lock_panel.rs`. Record evidence in ledger.md#w061.
- [ ] T070 [US4] W062: Use `Instant`/`Duration` the way the chat timeout immediately below already does: store `last_api_check: Option<Instant>` and `save_status_at: Option<Instant>`, and drive the pulse from elapsed seconds rather than `frame_count`. Target: `crates/addon/src/ui/main_view/mod.rs`. Record evidence in ledger.md#w062.
- [ ] T071 [US4] W063: Either set `names_loading = true` plus a `names_stage` message before the `spawn_worker` at stats.rs:146 (and handle the `!spawned` case like every other site in the file), or delete the three fields and this render branch. Target: `crates/addon/src/ui/main_view/mod.rs`. Record evidence in ledger.md#w063.
- [ ] T072 [US4] W064: If weight edits should invalidate results, use the flag the way the role/mode/scale handlers do; otherwise drop the binding and change the signature to `-> ()`. Target: `crates/addon/src/ui/main_view/mod.rs`. Record evidence in ledger.md#w064.
- [ ] T073 [US4] W065: Make `resolution::opt_stats_to_stat_block` `pub(super)` (or move it to a shared `stats` helper alongside `perf_to_combat_metrics`) and call it from all three optimization.rs sites. Target: `crates/addon/src/ui/main_view/optimization.rs`. Depends on B001. Record evidence in ledger.md#w065.
- [ ] T074 [US4] W066: `use gw2_optimizer::scoring::{STRIKE_DPS_NORM, CONDI_DPS_NORM};` and divide by those. Target: `crates/addon/src/ui/main_view/optimization.rs`. Record evidence in ledger.md#w066.
- [ ] T075 [US4] W067: Drop the parameter and return `DataQuality::Blocked` (keeping the doc comment as the justification), or compute `empty_kit` from the candidate at the call site so the branch becomes real. Target: `crates/addon/src/ui/main_view/optimization.rs`. Depends on B001. Record evidence in ledger.md#w067.
- [ ] T076 [US4] W068: Make `REFERENCE_WEAPON_STRENGTH` `pub` in `gw2_optimizer::combat` and use it here (and at engine.rs:1296 / gemini_tools.rs:1527). Target: `crates/addon/src/ui/main_view/optimization.rs`. Depends on W035. Record evidence in ledger.md#w068.
- [ ] T077 [US4] W069: Delete lines 1585-1589; `#[cfg(test)] mod tests` needs no doc comment and no clippy allow. Target: `crates/addon/src/ui/main_view/optimization.rs`. Record evidence in ledger.md#w069.
- [ ] T078 [US4] W070: Bind the result and log the `Err` at `LogLevel::Warning` (the same channel the tier fallbacks below already use) before dropping to `None`; consider pushing a `DataQualityReason` so the served suggestion says the advisor pass was skipped. Target: `crates/addon/src/ui/main_view/optimize_flow.rs`. Depends on W123. Record evidence in ledger.md#w070.
- [ ] T079 [US4] W071: Decide and encode it: either populate `traits_available` in `resolve_specs_db` from `spec.major_traits` so the fallbacks are real, or delete the field, both fallback loops, and the test that pretends they run. Target: `crates/addon/src/ui/main_view/resolution.rs`. Record evidence in ledger.md#w071.
- [ ] T080 [US4] W072: Match to `(set, is_main)` first — `"WeaponA1" => (&mut ws1, true)`, … — then run the shared body once against that pair. Target: `crates/addon/src/ui/main_view/resolution.rs`. Record evidence in ledger.md#w072.
- [ ] T081 [US4] W073: Move `format_timestamp` and `paint_row_plate` to a `pub(super)` helper module under main_view (or theme.rs for the plate) and delete the copies; replace the date arithmetic with `chrono::DateTime::<Utc>::from_timestamp(ts, 0).format("%Y-%m-%d %H:%M")`. Target: `crates/addon/src/ui/main_view/tabs/about.rs`. Record evidence in ledger.md#w073.
- [ ] T082 [US4] W074: Make `theme::clip_to_width` `pub(crate)`, replace its body with the O(n) accumulate-advances version from radio.rs:1635, and delete the four local copies. Target: `crates/addon/src/ui/main_view/tabs/about.rs`. Record evidence in ledger.md#w074.
- [ ] T083 [US4] W075: Delete about.rs `wrap_pos_local` and its test, point both `wrapped_to` copies at `theme::wrap_pos_local`, and promote one `wrapped_to` into theme.rs next to `theme::wrapped`. Target: `crates/addon/src/ui/main_view/tabs/about.rs`. Record evidence in ledger.md#w075.
- [ ] T084 [US4] W076: Delete the `FmtIcon::Bold` arm in `draw_fmt_icon` (or move the text-'B' rendering into it and drop the special case at line 546 so one path owns the icon). Target: `crates/addon/src/ui/main_view/tabs/about/wizard.rs`. Record evidence in ledger.md#w076.
- [ ] T085 [US4] W077: Add an explicit `label_kind: LabelKind` (or `Option<f64> score`) field on `BuildSuggestion` set at optimization.rs:462, and branch on that in improve.rs and comparison.rs; if the string protocol must stay, name the prefix once as a `pub const`. Target: `crates/addon/src/ui/main_view/tabs/improve.rs`. Record evidence in ledger.md#w077.
- [ ] T086 [US4] W078: Call `collected` once, keep `let total = all.len()` before filtering, pass `total` into `masthead`, and derive `empty` from `total == 0`. Target: `crates/addon/src/ui/main_view/tabs/news.rs`. Record evidence in ledger.md#w078.
- [ ] T087 [US4] W079: In both catch arms, extract the payload (`e.downcast_ref::<&str>()`/`String`) and `nexus::log::log(LogLevel::Critical, ..)` it before setting the user-facing message; better, route both through `spawn_flag_guarded` and log once in its `apply` on `Err(_)`. Target: `crates/addon/src/ui/main_view/tabs/radio.rs`. Record evidence in ledger.md#w079.
- [ ] T088 [US4] W080: Collapse both into one `save_current_suggestion(state, character_name)` and one `set_save_status(state, msg, is_err)`; call them from the New Build/Improve footer and the ranch corral bar. Target: `crates/addon/src/ui/main_view/tabs/saveload.rs`. Record evidence in ledger.md#w080.
- [ ] T089 [US4] W081: Add `theme::dimmed_button(ui, label, size, tip: Option<&str>)` (gold variant + a colour parameter for the amber Resend button) and replace all ten sites. Target: `crates/addon/src/ui/main_view/tabs/saveload.rs`. Record evidence in ledger.md#w081.
- [ ] T090 [US4] W083: Normalize legacy saved-build fields on load with explicit missing-profession handling; never silently assume Warrior or remove supported read compatibility. Target: `crates/addon/src/ui/main_view/tabs/saveload.rs`. Record evidence in ledger.md#w083.
- [ ] T091 [US4] W084: Add locale keys (`btn.show_key`, `btn.hide_key`, `err.thread_panicked`, `err.spawn_refused`, `ranch.corrupt_skipped`, `radio.error.search_failed`, `starter.*.prompt`) and route each site through `t()`/`tf()`. Target: `crates/addon/src/ui/main_view/tabs/settings.rs`. Record evidence in ledger.md#w084.
- [ ] T092 [US4] W085: In `render_model_picker_section` replace lines 520-525 with `state.config.active_model_id().to_string()` and lines 536-549 with `model_catalog(state)`. Target: `crates/addon/src/ui/main_view/tabs/settings.rs`. Record evidence in ledger.md#w085.
- [ ] T093 [US4] W086: Expose `LlmProvider::usage_file_name(&self) -> &'static str` (or a `pub fn usage_path(addon_dir, provider)`) in gw2_core::config or gw2_optimizer::llm and a typed `read_usage()` returning the count; use it from both the writer and settings.rs. Target: `crates/addon/src/ui/main_view/tabs/settings.rs`. Record evidence in ledger.md#w086.
- [ ] T094 [US4] W087: Consolidate with W074; verify this entry's distinct claim before closing. Target: `crates/addon/src/ui/news_feed.rs`. Depends on W074. Record evidence in ledger.md#w087.
- [ ] T095 [US4] W088: Delete `render_presets`; then `OptimizationWeights::PRESETS` and its six preset fns have no non-test consumer and can follow in the optimizer crate. Target: `crates/addon/src/ui/radar_chart.rs`. Record evidence in ledger.md#w088.
- [ ] T096 [US4] W089: Make `download::TOTAL_STEPS + NAME_STEPS` public as a single `pub const DOWNLOAD_STEPS: usize` in gw2api and use it here. Target: `crates/addon/src/ui/setup.rs`. Record evidence in ledger.md#w089.
- [ ] T097 [US4] W090: Add one `fn chip_colors(p: &Palette, selected: bool, hovered: bool) -> (fill, rim, text)` and one `fn draw_pill_rect(dl, p, w, h, fill, rim)`, and have all three call sites use them. Target: `crates/addon/src/ui/theme.rs`. Record evidence in ledger.md#w090.
- [ ] T098 [US4] W091: Add `setup.fetching`, `setup.speed_note`, `setup.tier_bronze/silver/gold/gem` (and `viability.gate.*`) to locales/*.json and call `t()` at these sites, as the rest of the wizard already does. Target: `crates/addon/src/ui/theme.rs`. Record evidence in ledger.md#w091.
- [ ] T099 [US4] W092: Either publish the limits from one place (a tiny shared crate, or have the client read them from the `/v1/taxonomy` response it already fetches), or add a test that fails when the two constant sets diverge. Target: `crates/core/src/feedback/report.rs`. Record evidence in ledger.md#w092.
- [ ] T100 [US4] W093: Delete `save` and `test_save_convenience_method`; callers already use the explicit `save_new` / `save_overwrite` pair. Target: `crates/core/src/storage.rs`. Record evidence in ledger.md#w093.
- [ ] T101 [US4] W094: Drop the prints (the skipped-name list and the returned error already carry the information to callers), or return the diagnostic so the addon can route it to the Nexus log / status bar. Target: `crates/core/src/storage.rs`. Record evidence in ledger.md#w094.
- [ ] T102 [US4] W095: Delete `describe_constraints` and its eight snapshot tests, or wire the live prompt builder to it so the format has exactly one owner. Target: `crates/core/src/types.rs`. Record evidence in ledger.md#w095.
- [ ] T103 [US4] W096: Extract one private `fn write_json_atomic<T: Serialize>(&self, key: &str, value: &T) -> Result<(), CacheError>` and have all three call it. Target: `crates/gw2api/src/cache.rs`. Record evidence in ledger.md#w096.
- [ ] T104 [US4] W097: Delete the method. Target: `crates/gw2api/src/cache.rs`. Record evidence in ledger.md#w097.
- [ ] T105 [US4] W098: Build the header once from the crate version, e.g. `HeaderValue::from_static(concat!("GW2BuildOptimizer/", env!("CARGO_PKG_VERSION")))`, and hoist it out of the per-attempt loop. Target: `crates/gw2api/src/client.rs`. Record evidence in ledger.md#w098.
- [ ] T106 [US4] W099: Either delete `validate_api_key` and `ApiError::MissingScopes`, or make `setup.rs` call it (returning the recommended-scope table it also needs) so the required-scope list exists once. Target: `crates/gw2api/src/client.rs`. Record evidence in ledger.md#w099.
- [ ] T107 [US4] W100: Delete the function, its call site wrapper (leave the `?` on `download_missing`), and the two tests; move the explanatory comment onto the `download_missing` call if it is worth keeping. Target: `crates/gw2api/src/download.rs`. Record evidence in ledger.md#w100.
- [ ] T108 [US4] W101: Make `API_LANGS` the single source: `if !API_LANGS.contains(&lang)` here, `API_LANGS.contains(&c)` in `with_lang`, and have `i18n::api_lang` consult the same list (or move the list into `gw2-core` since `gw2-api` already depends on it). Target: `crates/gw2api/src/localize.rs`. Record evidence in ledger.md#w101.
- [ ] T109 [US4] W102: Use the canonical Generic profile for missing scenario fallback and preserve explicit quality degradation; remove duplicated literal profile values. Target: `crates/optimizer/src/combat.rs`. Record evidence in ledger.md#w102.
- [ ] T110 [US4] W103: Delete the whole arm (the `_ => {}` below already covers it), or implement the buff handling. Target: `crates/optimizer/src/combat.rs`. Record evidence in ledger.md#w103.
- [ ] T111 [US4] W104: Move the prose-fallback coefficients, the trigger-cadence table, UPGRADE_BUFF_S and the per-sigil overrides into a data/ JSON with the same EvidenceLevel/sources treatment as the other formula files, or at minimum promote each to a named `const` with a doc comment stating it is an unsourced heuristic and what it was calibrated against. Target: `crates/optimizer/src/combat.rs`. Record evidence in ledger.md#w104.
- [ ] T112 [US4] W105: Replace the inline sanitizer with `crate::prompts::sanitize_build_summary(summary)` and drop the comment. Target: `crates/optimizer/src/context.rs`. Record evidence in ledger.md#w105.
- [ ] T113 [US4] W106: Consolidate with W014; verify this entry's distinct claim before closing. Target: `crates/optimizer/src/data/balance_overrides.rs`. Depends on W014. Record evidence in ledger.md#w106.
- [ ] T114 [US4] W107: Document provenance-only formula fields; consume genuinely behavioral suppression fields or reject unsupported controls at load, rather than silently advertising them. Target: `crates/optimizer/src/data/boon_condition_formulas.rs`. Record evidence in ledger.md#w107.
- [ ] T115 [US4] W108: Align data state variants with the real runtime initialization outcomes from W016. Target: `crates/optimizer/src/data/mod.rs`. Record evidence in ledger.md#w108.
- [ ] T116 [US4] W109: Consolidate with W017; verify this entry's distinct claim before closing. Target: `crates/optimizer/src/data/normalized_effects.rs`. Depends on W017. Record evidence in ledger.md#w109.
- [ ] T117 [US4] W110: Delete `steals_boon,` from the doc comment at line 120 (it is not a modeled operation), or add both the `valid_interaction_keys` entry and an `OperationType::StealsBoon` variant if boon-stealing is meant to be scored. Target: `crates/optimizer/src/data/objective_profiles.rs`. Record evidence in ledger.md#w110.
- [ ] T118 [US4] W111: Remove unused arithmetic/convenience operators after checking production and integration-test callers; preserve live FactualValue semantics. Target: `crates/optimizer/src/data/quality.rs`. Record evidence in ledger.md#w111.
- [ ] T119 [US4] W112: Make it `#[cfg(test)] fn`, or have `load_all_rotation_profiles` call it per-file with the real mode label so there is one parse/validate implementation and the tests exercise the production path. Target: `crates/optimizer/src/data/rotation_profiles.rs`. Record evidence in ledger.md#w112.
- [ ] T120 [US4] W113: Migrate the `condition_application` keys in the three rotation_profiles JSON files to canonical form and route `condition_weight` through `canonical_condition_name` like the sibling loaders; failing that, add a load-time validation that every `condition_application` key resolves via `canonical_condition_name` to a key present in `formulas/conditions.json`, so a rename fails loudly instead of silently scoring 0. Target: `crates/optimizer/src/data/rotation_profiles.rs`. Record evidence in ledger.md#w113.
- [ ] T121 [US4] W114: Move test-only convenience constructors behind test support while keeping production from_profile. Target: `crates/optimizer/src/data/rotation_profiles.rs`. Record evidence in ledger.md#w114.
- [ ] T122 [US4] W115: Replace both references with a self-contained statement of the invariant (e.g. "16 entries: 6 armor + 4 weapon slots across 2 sets + 6 trinkets; main-hand slots draw the two-hand budget"), and drop the "replaces the old ..." sentence at line 209. Target: `crates/optimizer/src/data/slot_budgets.rs`. Record evidence in ledger.md#w115.
- [ ] T123 [US4] W116: Remove unused budget accessors after current reference checks; preserve the shape-aware path in use. Target: `crates/optimizer/src/data/slot_budgets.rs`. Record evidence in ledger.md#w116.
- [ ] T124 [US4] W117: Use the canonical health formula helper; remove unused strike helper only if the live call shape cannot use it without behavior changes. Target: `crates/optimizer/src/data/universal_formulas.rs`. Record evidence in ledger.md#w117.
- [ ] T125 [US4] W118: Hoist one `struct PrecomputedSpec` to module scope and extract a single `fn precompute_specs(spec_combos, specs_cache, traits_cache, items_cache, stat_weights, locks, ctx) -> Vec<PrecomputedSpec>`, called by both paths with `&db.items` / `&HashMap::new()` respectively. Target: `crates/optimizer/src/engine.rs`. Record evidence in ledger.md#w118.
- [ ] T126 [US4] W119: Run `cargo fmt --all` once across the workspace and commit the result as a single formatting-only change (so it does not pollute future git blame on logic edits), then confirm `cargo fmt --all --check` exits clean. Target: `crates/optimizer/src/engine.rs`. Record evidence in ledger.md#w119.
- [ ] T127 [US4] W120: Read the divisor from the formula table: `crate::data::universal_formulas::formulas().ferocity_per_crit_damage_pct` instead of `15.0`. Target: `crates/optimizer/src/engine.rs`. Depends on W035. Record evidence in ledger.md#w120.
- [ ] T128 [US4] W121: Drop dead arguments through callers; keep actually-used caches correctly named. Target: `crates/optimizer/src/engine.rs`. Record evidence in ledger.md#w121.
- [ ] T129 [US4] W122: Delete `#[allow(clippy::too_many_arguments)]` at engine.rs:2285 (llm_advisor already has its own at 2349) and delete `#[allow(clippy::field_reassign_with_default)]` at validation.rs:434. Target: `crates/optimizer/src/engine.rs`. Record evidence in ledger.md#w122.
- [ ] T130 [US4] W123: Capture the error and surface it through the existing result channels: in `llm_advisor` return a `(ValidatedBuild, Option<LlmError>)` or push onto `current.warnings` before returning; in `optimize_deterministic_cancellable` push a `data::DataQualityReason { field: "llm.explanation", .. }` (or a `result.validated.warnings` entry) carrying the provider message, so the About/Improve surfaces can show 'explanation unavailable: <reason>'. Target: `crates/optimizer/src/engine.rs`. Record evidence in ledger.md#w123.
- [ ] T131 [US4] W124: Normalize the old advisor token syntax at one parsing boundary and keep one branch, retaining compatibility coverage. Target: `crates/optimizer/src/engine.rs`. Record evidence in ledger.md#w124.
- [ ] T132 [US4] W125: Move lines 742-744 down to sit above `#[cfg(test)] pub(crate) mod tests_alias_helpers` at gamedb.rs:755, leaving `profession_skill_index`'s doc starting at the (correct) 'Skills per profession...' line. Target: `crates/optimizer/src/gamedb.rs`. Record evidence in ledger.md#w125.
- [ ] T133 [US4] W126: Delete it; callers that want the no-progress form already go through llm::generate_with_tools. Target: `crates/optimizer/src/gemini.rs`. Record evidence in ledger.md#w126.
- [ ] T134 [US4] W127: Build the snapshot under the lock and drop it before writing: `let persisted = { let rate = self.rate.lock()...; rate.to_persisted() }; self.persist_usage(&persisted);` — i.e. change persist_usage to take PersistedUsage rather than &RateTracker. Target: `crates/optimizer/src/gemini.rs`. Record evidence in ledger.md#w127.
- [ ] T135 [US4] W128: Consolidate with W029; verify this entry's distinct claim before closing. Target: `crates/optimizer/src/gemini.rs`. Depends on W029. Record evidence in ledger.md#w128.
- [ ] T136 [US4] W129: Replace the claim with the real policy — results are capped by count (12/15/20/5) and the conversation is trimmed by gemini::trim_contents — or add an actual token/byte cap to the exec handlers if the 500-token contract is still wanted. Target: `crates/optimizer/src/gemini_tools.rs`. Record evidence in ledger.md#w129.
- [ ] T137 [US4] W130: Delete `impl NamedRecord for ItemStat` (lines 27-34) and drop the ItemStat import if it becomes unused; the trait doc at line 20 should stop naming ItemStat. Target: `crates/optimizer/src/gemini_tools.rs`. Record evidence in ledger.md#w130.
- [ ] T138 [US4] W131: Extract the prefix -> (itemstat, full_stats, derived) prologue into one helper returning Result<_, Value> so both execs share the error strings, and route all three combat renderings through format_combat_performance (with a field subset argument if the shorter forms are deliberate). Target: `crates/optimizer/src/gemini_tools.rs`. Record evidence in ledger.md#w131.
- [ ] T139 [US4] W132: Include `stop_reason` in the empty-content error text like the OpenAI path does and drop the allow; delete the `created` field from openai.rs's `ModelEntry` (serde ignores unknown fields by default). Target: `crates/optimizer/src/llm/anthropic.rs`. Record evidence in ledger.md#w132.
- [ ] T140 [US4] W133: Consolidate with W132; verify this entry's distinct claim before closing. Target: `crates/optimizer/src/llm/anthropic.rs`. Depends on W132. Record evidence in ledger.md#w133.
- [ ] T141 [US4] W134: Consolidate with W027; verify this entry's distinct claim before closing. Target: `crates/optimizer/src/llm/anthropic.rs`. Depends on W027. Record evidence in ledger.md#w134.
- [ ] T142 [US4] W135: Use the real `status` in the error, make the 403 policy identical in both methods (or let `validate_key` delegate to `validate_key_detailed` and map `valid` back to Ok/Err), and replace the inline word list with `has_billing_keyword`. Target: `crates/optimizer/src/llm/anthropic.rs`. Record evidence in ledger.md#w135.
- [ ] T143 [US4] W136: Reference the constant by name without repeating its value, or restate the arithmetic against the current 65_536 (and note that MAX_LLM_BODY must be revisited if it grows again); fix anthropic.rs:34 the same way. Target: `crates/optimizer/src/llm/body.rs`. Record evidence in ledger.md#w136.
- [ ] T144 [US4] W137: Delete `body_capped` in llm/gemini.rs, the three `.and_then(body_capped)` calls (lines 184, 191, 212), the `MAX_LLM_BODY` import and the test at 336-342; rely on crate::gemini's reader cap. Target: `crates/optimizer/src/llm/gemini.rs`. Record evidence in ledger.md#w137.
- [ ] T145 [US4] W138: Consolidate with W137; verify this entry's distinct claim before closing. Target: `crates/optimizer/src/llm/gemini.rs`. Depends on W137. Record evidence in ledger.md#w138.
- [ ] T146 [US4] W139: Delete `raw()` and its doc comment. Target: `crates/optimizer/src/llm/gemini.rs`. Record evidence in ledger.md#w139.
- [ ] T147 [US4] W140: Consolidate with W139; verify this entry's distinct claim before closing. Target: `crates/optimizer/src/llm/gemini.rs`. Depends on W139. Record evidence in ledger.md#w140.
- [ ] T148 [US4] W141: Override generate_brief in AnthropicClient with send_messages(&messages, None, None, max_tokens.min(ANTHROPIC_MAX_TOKENS)); correct the trait doc to name Gemini as the only provider that falls back. Target: `crates/optimizer/src/llm/mod.rs`. Record evidence in ledger.md#w141.
- [ ] T149 [US4] W142: Consolidate with W029; verify this entry's distinct claim before closing. Target: `crates/optimizer/src/llm/mod.rs`. Depends on W029. Record evidence in ledger.md#w142.
- [ ] T150 [US4] W143: Delete the function. Target: `crates/optimizer/src/llm/mod.rs`. Record evidence in ledger.md#w143.
- [ ] T151 [US4] W144: Add rate::load_usage(path: &Path, rpm_limit: u32) -> RateTracker next to persist_usage, have all four constructors call it, and surface a parse failure through the Nexus log at Warning rather than .ok(). Target: `crates/optimizer/src/llm/openai.rs`. Record evidence in ledger.md#w144.
- [ ] T152 [US4] W145: Replace each inline chain with `super::has_billing_keyword(&body)`; add a `models_request()` helper to openai.rs and openrouter.rs like anthropic.rs has. Target: `crates/optimizer/src/llm/openai.rs`. Record evidence in ledger.md#w145.
- [ ] T153 [US4] W146: Consolidate with W145; verify this entry's distinct claim before closing. Target: `crates/optimizer/src/llm/openai.rs`. Depends on W145. Record evidence in ledger.md#w146.
- [ ] T154 [US4] W147: Change the comment to 'Sort alphabetically by id' like the other two providers, or implement the ranking the comment describes. Target: `crates/optimizer/src/llm/openai.rs`. Record evidence in ledger.md#w147.
- [ ] T155 [US4] W148: Consolidate with W147; verify this entry's distinct claim before closing. Target: `crates/optimizer/src/llm/openai.rs`. Depends on W147. Record evidence in ledger.md#w148.
- [ ] T156 [US4] W149: Delete `EHP_FLOOR_WVW` and its doc line. Target: `crates/optimizer/src/referee.rs`. Record evidence in ledger.md#w149.
- [ ] T157 [US4] W150: Delete `build_rotation_skills`. Target: `crates/optimizer/src/rotation/builder.rs`. Record evidence in ledger.md#w150.
- [ ] T158 [US4] W151: Label fallback barrier coefficients as heuristic and include them in coverage; do not invent authoritative sourced coefficients. Target: `crates/optimizer/src/rotation/builder.rs`. Record evidence in ledger.md#w151.
- [ ] T159 [US4] W152: Call `strip_total(effect)` from wvw_timeline.rs:1157 and from builder.rs:435 (the latter needs a small helper taking the three numbers, since it has no SkillEffect yet), then delete the two inline copies and the unreachable `.unwrap_or(0)`. Target: `crates/optimizer/src/rotation/mod.rs`. Record evidence in ledger.md#w152.
- [ ] T160 [US4] W153: Extract one `fn strike_damage(params: &SimParams, might_stacks: f64, fury: bool, hit_count: u32, dmg_multiplier: f64) -> f64` in simulator.rs, make it `pub(super)` so wvw_timeline can use it too, and have all four sites call it. Target: `crates/optimizer/src/rotation/simulator.rs`. Record evidence in ledger.md#w153.
- [ ] T161 [US4] W154: Expose useful simulation counters via quality/diagnostic report consumers and remove only counters confirmed redundant. Target: `crates/optimizer/src/rotation/wvw_timeline.rs`. Record evidence in ledger.md#w154.
- [ ] T162 [US4] W155: Pass `weapon_swap_cooldown_ms` through `Timeline::new` — or better, hand `Timeline::new` the whole `WvwTimelineInput` (it already owns every other argument), which removes both the post-hoc assignment and the `too_many_arguments` allow. Target: `crates/optimizer/src/rotation/wvw_timeline.rs`. Record evidence in ledger.md#w155.
- [ ] T163 [US4] W156: Hoist it to one `const ENEMY_CONDITION_DAMAGE: f64 = 1_800.0;` next to the other consts at the top of the file with a note on where the number came from, or better, derive it from `WvwProfile`'s `pressure` the same way `enemy_power` is, and store it on the profile. Target: `crates/optimizer/src/rotation/wvw_timeline.rs`. Record evidence in ledger.md#w156.
- [ ] T164 [US4] W158: Remove unused target-profile state if fresh callers confirm no behavior; do not change existing cleave semantics. Target: `crates/optimizer/src/scenario.rs`. Record evidence in ledger.md#w158.
- [ ] T165 [US4] W159: Remove unreachable role variants after verifying serialized compatibility and profile reachability; keep a read migration if serialized. Target: `crates/optimizer/src/scenario.rs`. Record evidence in ledger.md#w159.
- [ ] T166 [US4] W160: Delete `profile_id`, `to_weights`, `RoleObjective::combat_tier`, `with_combat_kind` and `CombatKind::label`; rewrite the handful of tests that call them (scenario.rs:377-421, :425-462, :537-552) to use `profile_id_for`/`to_weights_for` with an explicit tier, which is what production does and therefore what should be under test. Target: `crates/optimizer/src/scenario.rs`. Record evidence in ledger.md#w160.
- [ ] T167 [US4] W161: Rewrite both comments to state what is true today — presets are hardcoded and are what the UI uses; profile data currently supplies only `default_for_mode` and the (uncalled) ObjectiveScorer — or finish the migration so the presets really come from objective_profiles data. Target: `crates/optimizer/src/scoring.rs`. Record evidence in ledger.md#w161.
- [ ] T168 [US4] W162: Either call `format_source_status` from settings.rs (it is better factored than the inline formatting there) or delete it with its tests. Target: `crates/optimizer/src/scraper.rs`. Record evidence in ledger.md#w162.
- [ ] T169 [US4] W163: Add `fn all_failed(msg: &str, on_progress: &dyn Fn(&str, &str)) -> Vec<ScrapeResult>` alongside the existing `cancelled_result`, driven by a single `const SOURCES: [&str; 3] = ["snowcrows", "hardstuck", "guildjen"]`, and have all three early-return blocks (plus `cancelled_result`'s callers) go through it. Target: `crates/optimizer/src/scraper.rs`. Record evidence in ledger.md#w163.
- [ ] T170 [US4] W164: Declare one `const PROFESSIONS: &[&str]` at module scope, point `scrape_snowcrows`, `scrape_hardstuck` and `parse_profession_spec_from_url` at it, and derive `CORE_PROFESSIONS` from it via the existing `title_case` helper rather than re-typing the title-cased spellings. Target: `crates/optimizer/src/scraper.rs`. Record evidence in ledger.md#w164.
- [ ] T171 [US4] W165: Delete `strip_tags` and its test (scraper.rs:1416-1420), `use crate::text_util::strip_gw2_markup;` and call that at the one use site (scraper.rs:1103). Target: `crates/optimizer/src/scraper.rs`. Record evidence in ledger.md#w165.
- [ ] T172 [US4] W166: Reword both docs to reference SearchConfig::default() symbolically ('the beam's eval_budget/time_limit') rather than restating numbers that have since changed. Target: `crates/optimizer/src/search_v2.rs`. Record evidence in ledger.md#w166.
- [ ] T173 [US4] W167: Make it #[cfg(test)] (or move the interleave into the test module) and rewrite the doc to point at generate_neighbor_groups + group_quotas as the live admission path. Target: `crates/optimizer/src/search_v2.rs`. Record evidence in ledger.md#w167.
- [ ] T174 [US4] W168: Add an explicit `diagnostic: bool` (or a separate `on_diagnostic` callback) to OptimizeProgress instead of prefix sniffing, and gate the fn_* histogram behind that or remove it now that the 2026-09-04 question is answered. Target: `crates/optimizer/src/search_v2.rs`. Record evidence in ledger.md#w168.
- [ ] T175 [US4] W169: Delete the '**or** the slot is None' clause from the doc (the strict rule matches eligible_slot_skills and refill_bar). Target: `crates/optimizer/src/search_v2.rs`. Record evidence in ledger.md#w169.
- [ ] T176 [US4] W170: Route swap_utility_skills, swap_utilities_for_failed_gates and refill_bar through eligible_slot_skills(candidate, db, prof, "Utility") and one shared `propose_utility_swaps(build, choices, held)` helper. Target: `crates/optimizer/src/search_v2.rs`. Record evidence in ledger.md#w170.
- [ ] T177 [US4] W171: Name it (e.g. Target: `crates/optimizer/src/search_v2.rs`. Record evidence in ledger.md#w171.
- [ ] T178 [US4] W172: Delete the eight unused methods and the tests that exist only for them (`seats_round_trip_through_their_weapon_slot`, and the `as_array`/`count_equipped`/`equipped`/`second_set` assertions inside the remaining tests). Target: `crates/optimizer/src/sigil_slots.rs`. Record evidence in ledger.md#w172.
- [ ] T179 [US4] W173: Remove confirmed unused stat APIs and exclusive tests, preserving production duration helpers and regression coverage. Target: `crates/optimizer/src/stats.rs`. Record evidence in ledger.md#w173.
- [ ] T180 [US4] W174: Delete `calculate_trait_stats`, port its three tests onto `calculate_trait_stats_for_mode(..., &GameMode::PvE)`, and delete the two source-scanning guard tests in engine.rs and synergy_pipeline.rs that only exist to police it. Target: `crates/optimizer/src/stats.rs`. Record evidence in ledger.md#w174.
- [ ] T181 [US4] W175: Move the table to data/trait_mode_splits.json with the project's standard loader (include_str! + OnceLock + validation) and per-row `sources`/`evidence_level`, matching profession_profiles.json. Target: `crates/optimizer/src/stats.rs`. Record evidence in ledger.md#w175.
- [ ] T182 [US4] W176: Keep `impl AddAssign<&StatBlock>` as the single implementation; replace the inline block at L601-609 with `stats += &gear;`, replace `add_block` calls with `+=` and delete it, and delete the unused by-value `impl AddAssign` (see the dead-items finding). Target: `crates/optimizer/src/stats.rs`. Record evidence in ledger.md#w176.
- [ ] T183 [US4] W177: Trim unused synergy-link metadata after checking graph/UI consumers; preserve score and explanation. Target: `crates/optimizer/src/synergy.rs`. Record evidence in ledger.md#w177.
- [ ] T184 [US4] W178: Have extract_sigil_effects call combat::parse-side logic (apply the shared DamageModifiers path via effects_from_upgrade_text on the same text combat.rs uses) so the fallback table lives once, or move the two numbers into data/ with the other balance constants. Target: `crates/optimizer/src/synergy.rs`. Record evidence in ledger.md#w178.
- [ ] T185 [US4] W179: Delete the field and the #[allow], or use it in select_weapons in place of the db lookup. Target: `crates/optimizer/src/synergy_pipeline.rs`. Record evidence in ledger.md#w179.
- [ ] T186 [US4] W180: Rewrite the doc: 'Non-cancellable convenience wrapper used by examples and tests; production callers use optimize_synergy_cancellable.' Target: `crates/optimizer/src/synergy_pipeline.rs`. Record evidence in ledger.md#w180.
- [ ] T187 [US4] W181: Extract `best_land_set(available, weapon_scores, exclude_main: Option<&str>) -> ((Option<String>, Option<String>), f64)` and call it twice. Target: `crates/optimizer/src/synergy_pipeline.rs`. Record evidence in ledger.md#w181.
- [ ] T188 [US4] W183: Route saved-build conversion through the existing canonical slot migration, then remove redundant uncalled backfill helper. Target: `crates/optimizer/src/validation.rs`. Record evidence in ledger.md#w183.
- [ ] T189 [US4] W184: Retain disable alias for saved-build reads; canonicalize on save and test the migration. Target: `crates/optimizer/tests/objective_profiles_integration.rs`. Depends on W083. Record evidence in ledger.md#w184.
- [ ] T190 [US4] W185: Mark the plan as a historical artifact (status header + 'code blocks are the 2026-08-24 snapshot, not current') or strip the inline source blocks and point at server/feedback/src/ instead; at minimum fix the XFF and auto-read lines, which contradict deliberate later fixes. Target: `docs/superpowers/plans/2026-08-24-feedback-server.md`. Record evidence in ledger.md#w185.
- [ ] T191 [US4] W186: Delete lines 139-141 and either delete or properly format lines 118-137 as the review-findings appendix they are meant to be. Target: `docs/superpowers/plans/2026-08-26-per-slot-gear-implementation.md`. Record evidence in ledger.md#w186.
- [ ] T192 [US4] W187: Add a short 'as-built deltas' section to the spec (or update §2, §6, §6a and §7 in place) recording: read is explicit via POST /reports/read, a session-cookie admin page ships, and queries are runtime-bound by design. Target: `docs/superpowers/specs/2026-08-24-feedback-and-about-design.md`. Record evidence in ledger.md#w187.
- [ ] T193 [US4] W188: Either execute Task 7 of the implementation plan (stop writing the legacy fields, keep only legacy deserialization) or update the spec to state that both shapes are permanent and say which is authoritative. Target: `docs/superpowers/specs/2026-08-26-per-slot-gear-design.md`. Depends on W083. Record evidence in ledger.md#w188.
- [ ] T194 [US4] W189: Add locale reachability/parity validation with an explicit dynamic-key inventory; remove only keys proven unreachable. Target: `locales/en.json`. Depends on W084, W091, W059. Record evidence in ledger.md#w189.
- [ ] T195 [US4] W190: Populate the category <select> at load time from `GET /v1/taxonomy` (the endpoint is already public and unauthenticated), falling back to the current static list if the fetch fails. Target: `server/feedback/src/admin.html`. Record evidence in ledger.md#w190.
- [ ] T196 [US4] W191: Extract one `const LINE_RE` plus a single parse/pack pair and have `renderBody` render from the parsed object instead of re-matching. Target: `server/feedback/src/admin.html`. Record evidence in ledger.md#w191.
- [ ] T197 [US4] W192: Route lines 308-309 through the same handler used on line 307 (extract it into a named `reload()` and reuse it for the Reload button, the selects, and the search box). Target: `server/feedback/src/admin.html`. Record evidence in ledger.md#w192.
- [ ] T198 [US4] W193: Hash first: `let hash = ip_hash(&ip, &s.config.ip_salt, Utc::now().date_naive());` and key on `login:{hash}`, matching reports.rs. Target: `server/feedback/src/admin.rs`. Record evidence in ledger.md#w193.
- [ ] T199 [US4] W194: Parse into a fixed (major, minor, patch) tuple with an explicit error - reject a header that does not parse with `ApiError::BadRequest` rather than filtering components away, and zero-extend a short version instead of comparing ragged vectors. Target: `server/feedback/src/reports.rs`. Record evidence in ledger.md#w194.

## Phase 7: US5 — Resolve S4 and contested entries (P5)

Independent acceptance: see spec.md US5; each item must satisfy its own ledger acceptance and dependencies. Write behavioral regression tests before the corresponding fix. Exact target is the source path below; use SymForge to resolve current symbols rather than historical line numbers.

- [ ] T200 [US5] W195: Add `const MAX_BODY_BYTES: u64 = 1024 * 1024;` next to `TIMEOUT`/`MAX_REASON_CHARS` at the top of client.rs and use it at all three sites; move `MAX_POLL_IDS` into client.rs (or import it from tasks.rs) so `.take(MAX_POLL_IDS)` and `pollable_ids` read the same constant. Target: `crates/addon/src/feedback/client.rs`. Record evidence in ledger.md#w195.
- [ ] T201 [US5] W196: Add `pub const COUNT: usize` to `NewsSource` beside `index()` and declare the arrays as `[_; NewsSource::COUNT]`, so adding a variant either compiles correctly or fails at the definition rather than at runtime. Target: `crates/addon/src/news.rs`. Record evidence in ledger.md#w196.
- [ ] T202 [US5] W197: Review the documented deliberate limitation against current code; retain with rationale and a concrete reconsideration trigger if still valid. Target: `crates/addon/src/radio/logos.rs`. Record evidence in ledger.md#w197.
- [ ] T203 [US5] W198: Review the documented deliberate limitation against current code; retain with rationale and a concrete reconsideration trigger if still valid. Target: `crates/addon/src/state.rs`. Record evidence in ledger.md#w198.
- [ ] T204 [US5] W199: Replace the inline block with `trim_history(&mut hist);`. Target: `crates/addon/src/ui/chat_bar.rs`. Record evidence in ledger.md#w199.
- [ ] T205 [US5] W200: Drop the parameter and the argument at line 585. Target: `crates/addon/src/ui/comparison.rs`. Record evidence in ledger.md#w200.
- [ ] T206 [US5] W201: Derive the predicate from `LATIN_RANGES` (iterate the pairs until the 0 terminator) so there is one source of truth, or add a test asserting the two agree on the boundary code points of every pair. Target: `crates/addon/src/ui/fonts.rs`. Record evidence in ledger.md#w201.
- [ ] T207 [US5] W202: Review the documented deliberate limitation against current code; retain with rationale and a concrete reconsideration trigger if still valid. Target: `crates/addon/src/ui/fonts.rs`. Record evidence in ledger.md#w202.
- [ ] T208 [US5] W203: Track as debt; when `ui/comparison.rs` and `locales/` are next opened, do (3) and (2)/(4) together, since translating `KEPT_GEAR_HEADLINE` is precisely what invalidates `from_label`. Target: `crates/addon/src/ui/main_view/optimize_flow.rs`. Depends on W077. Record evidence in ledger.md#w203.
- [ ] T209 [US5] W204: Pick one — the registered `"GW2 Build Optimizer"` from lib.rs:26 is the obvious canonical choice — put it behind a `const LOG_CHANNEL: &str` in the addon crate root, and use it everywhere. Target: `crates/addon/src/ui/main_view/optimize_flow.rs`. Record evidence in ledger.md#w204.
- [ ] T210 [US5] W205: Delete the wrapper and point mod.rs's three call sites at `resolve_selected_build_inner`, or rename the inner to `resolve_selected_build` and drop the `_inner` suffix everywhere. Target: `crates/addon/src/ui/main_view/resolution.rs`. Record evidence in ledger.md#w205.
- [ ] T211 [US5] W206: Add `about`, `news`, and `radio` bullets to the list (or drop the list and let the `pub(super) mod` lines speak). Target: `crates/addon/src/ui/main_view/tabs/mod.rs`. Record evidence in ledger.md#w206.
- [ ] T212 [US5] W207: Rename the locals to `frame`/`frames` (the sibling `toggle_favorite` already uses `now_frames`). Target: `crates/addon/src/ui/main_view/tabs/radio.rs`. Record evidence in ledger.md#w207.
- [ ] T213 [US5] W208: Replace each literal with the matching token (`theme::OPTIMIZED`, `theme::WARN`, `theme::ERR`, `theme::pal().gold/muted`), adding a `theme::INFO` if a blue accent is really wanted. Target: `crates/addon/src/ui/main_view/tabs/settings.rs`. Record evidence in ledger.md#w208.
- [ ] T214 [US5] W209: `let size = avail_w.min((avail_h - 48.0).max(176.0));` Target: `crates/addon/src/ui/radar_chart.rs`. Record evidence in ledger.md#w209.
- [ ] T215 [US5] W210: Review the documented deliberate limitation against current code; retain with rationale and a concrete reconsideration trigger if still valid. Target: `crates/core/src/config.rs`. Record evidence in ledger.md#w210.
- [ ] T216 [US5] W211: Confirm whether `AppConfig::save` still runs on the render thread; if not, add `File::sync_all` in the staging write the way `storage.rs::write_durably` does. Target: `crates/core/src/config.rs`. Depends on W009. Record evidence in ledger.md#w211.
- [ ] T217 [US5] W212: Review the documented deliberate limitation against current code; retain with rationale and a concrete reconsideration trigger if still valid. Target: `crates/core/src/storage.rs`. Record evidence in ledger.md#w212.
- [ ] T218 [US5] W213: Reword to describe what is there: a minimal `Meta` struct that ignores unknown fields by default, read through a `BufReader` so the 50 MB items cache is never materialized as a `String`. Target: `crates/gw2api/src/cache.rs`. Record evidence in ledger.md#w213.
- [ ] T219 [US5] W214: Review the documented deliberate limitation against current code; retain with rationale and a concrete reconsideration trigger if still valid. Target: `crates/gw2api/src/graphics.rs`. Record evidence in ledger.md#w214.
- [ ] T220 [US5] W215: Delete the alias, call `ids_from_cache_u32` from `fetch_str` and the test, and rename it to something type-neutral such as `ids_from_cache`. Target: `crates/gw2api/src/localize.rs`. Record evidence in ledger.md#w215.
- [ ] T221 [US5] W216: Delete the `on_progress` closure and its `let _ =`; change the match arm to `_ => OptimizationWeights { ... }`; change `forced_names` to `[&str; 3]` and iterate `for name in forced_names`. Target: `crates/optimizer/examples/nudge_druid_check.rs`. Record evidence in ledger.md#w216.
- [ ] T222 [US5] W217: Review the documented deliberate limitation against current code; retain with rationale and a concrete reconsideration trigger if still valid. Target: `crates/optimizer/src/data/cleanse_sources.rs`. Record evidence in ledger.md#w217.
- [ ] T223 [US5] W218: Validate them instead of suppressing — assert `rarity == "Ascended" && level == 80` in `validate_entries` and drop both `#[allow(dead_code)]` attributes; the fields then earn their place and the file's provenance is checked at load. Target: `crates/optimizer/src/data/slot_budgets.rs`. Record evidence in ledger.md#w218.
- [ ] T224 [US5] W219: Gate synthetic database constructor through test-support and addon dev dependency, verifying release feature resolution. Target: `crates/optimizer/src/gamedb.rs`. Record evidence in ledger.md#w219.
- [ ] T225 [US5] W220: Keep one const (e.g. Target: `crates/optimizer/src/gemini.rs`. Record evidence in ledger.md#w220.
- [ ] T226 [US5] W221: Review the documented deliberate limitation against current code; retain with rationale and a concrete reconsideration trigger if still valid. Target: `crates/optimizer/src/gemini.rs`. Record evidence in ledger.md#w221.
- [ ] T227 [US5] W222: Inline db.itemstat_by_name at the seven call sites and delete the wrapper; the four determinism tests belong next to GameDb::itemstat_by_name. Target: `crates/optimizer/src/gemini_tools.rs`. Record evidence in ledger.md#w222.
- [ ] T228 [US5] W223: Drop the parameter and the request field (or actually route the system portion of the prompt through it and keep both). Target: `crates/optimizer/src/llm/anthropic.rs`. Record evidence in ledger.md#w223.
- [ ] T229 [US5] W224: Consolidate with W136; verify this entry's distinct claim before closing. Target: `crates/optimizer/src/llm/body.rs`. Depends on W136. Record evidence in ledger.md#w224.
- [ ] T230 [US5] W226: Rewrite as past tense: 'Anthropic and Gemini each leaked a slot on mid-stream failure (GLM F16) until they adopted this guard.' Target: `crates/optimizer/src/llm/openai_compat.rs`. Record evidence in ledger.md#w226.
- [ ] T231 [US5] W227: Return rate persistence failures to a bounded diagnostic surface without exposing provider keys or response bodies. Target: `crates/optimizer/src/llm/rate.rs`. Record evidence in ledger.md#w227.
- [ ] T232 [US5] W228: Review the documented deliberate limitation against current code; retain with rationale and a concrete reconsideration trigger if still valid. Target: `crates/optimizer/src/llm/response_cache.rs`. Record evidence in ledger.md#w228.
- [ ] T233 [US5] W229: Consolidate with W228; verify this entry's distinct claim before closing. Target: `crates/optimizer/src/llm/response_cache.rs`. Depends on W228. Record evidence in ledger.md#w229.
- [ ] T234 [US5] W230: Reword sse.rs:9-10 to 'Anthropic streams a different event shape; see `anthropic::read_anthropic_stream`' and change openai_compat.rs:75-76 to past tense ('used to leak'). Target: `crates/optimizer/src/llm/sse.rs`. Record evidence in ledger.md#w230.
- [ ] T235 [US5] W232: Have `gemini_tools` (or a new `tools` module) define `Vec<ToolDefinition>` directly and derive Gemini's `FunctionDeclaration` from it in `to_gemini_tools`; rewrite the module doc to describe the current design. Target: `crates/optimizer/src/llm/tools.rs`. Record evidence in ledger.md#w232.
- [ ] T236 [US5] W233: Trim tools.rs to 'Converts gemini_tools::tool_declarations() into provider-neutral ToolDefinitions' and change sse.rs to state that Anthropic has a separate reader sharing the index cap. Target: `crates/optimizer/src/llm/tools.rs`. Record evidence in ledger.md#w233.
- [ ] T237 [US5] W234: Change to `(see the `expected` field on [`Case`])`, or rename `Case` to `Expectation` if that was the intent. Target: `crates/optimizer/src/parser_consistency_tests.rs`. Record evidence in ledger.md#w234.
- [ ] T238 [US5] W235: Drop the trailing clause from line 313 (line 311 already states it, and the JSON schema restates it) and update the snapshot literal in the same change. Target: `crates/optimizer/src/prompts.rs`. Record evidence in ledger.md#w235.
- [ ] T239 [US5] W236: Drop `"Might"` from the arm and reword the doc to say Might is handled entirely by the callers, not scaled on top of this function. Target: `crates/optimizer/src/rotation/simulator.rs`. Record evidence in ledger.md#w236.
- [ ] T240 [US5] W237: Reword to 'The DPCT rotation simulator answers ...' or 'The dummy-target simulator answers ...'. Target: `crates/optimizer/src/rotation/wvw_timeline.rs`. Record evidence in ledger.md#w237.
- [ ] T241 [US5] W238: Review the documented deliberate limitation against current code; retain with rationale and a concrete reconsideration trigger if still valid. Target: `crates/optimizer/src/rotation/wvw_timeline.rs`. Record evidence in ledger.md#w238.
- [ ] T242 [US5] W239: Pick one name. Target: `crates/optimizer/src/scoring.rs`. Record evidence in ledger.md#w239.
- [ ] T243 [US5] W240: Import the validation.rs consts in search.rs and delete the local copies. Target: `crates/optimizer/src/search.rs`. Record evidence in ledger.md#w240.
- [ ] T244 [US5] W241: Move the comment to sit above `let mut fn_generated` and finish the sentence ('admission was the bottleneck: ...'). Target: `crates/optimizer/src/search_v2.rs`. Record evidence in ledger.md#w241.
- [ ] T245 [US5] W242: Drop the numbers from the docs (the generate_neighbor_groups push order is the authoritative list). Target: `crates/optimizer/src/search_v2.rs`. Record evidence in ledger.md#w242.
- [ ] T246 [US5] W243: Add one `canonical_attribute(&str) -> Option<&'static str>` in stats.rs and derive StatType::from_api and the engine.rs match from it. Target: `crates/optimizer/src/synergy.rs`. Record evidence in ledger.md#w243.
- [ ] T247 [US5] W244: Delete it, or have upgrade_graph::stat_key reuse it. Target: `crates/optimizer/src/synergy.rs`. Record evidence in ledger.md#w244.
- [ ] T248 [US5] W245: Take `new_id: &ComponentId` and drop the closure fallback. Target: `crates/optimizer/src/synergy.rs`. Record evidence in ledger.md#w245.
- [ ] T249 [US5] W246: Delete combo_count/combo_limit and the take(), or make the per-spec top-N a named const and derive the cap from it. Target: `crates/optimizer/src/synergy_pipeline.rs`. Record evidence in ledger.md#w246.
- [ ] T250 [US5] W247: One generic `best_by_synergy<T>(items, extract: impl Fn(&T)->Vec<NormalizedEffect>, id: impl Fn(&T)->ComponentId, weights, accumulated)` used by all four. Target: `crates/optimizer/src/synergy_pipeline.rs`. Record evidence in ledger.md#w247.
- [ ] T251 [US5] W248: Consolidate with W176; verify this entry's distinct claim before closing. Target: `crates/optimizer/src/synergy_pipeline.rs`. Depends on W176. Record evidence in ledger.md#w248.
- [ ] T252 [US5] W249: Remove the parameter from the signature and both call sites. Target: `crates/optimizer/src/synergy_pipeline.rs`. Record evidence in ledger.md#w249.
- [ ] T253 [US5] W250: Delete both, and the `slot_count` assertions inside `slot_count_matches_the_slots_listed` (weapon_budget.rs:343-347), keeping the `is_two_handed` assertions there. Target: `crates/optimizer/src/upgrade_graph.rs`. Record evidence in ledger.md#w250.
- [ ] T254 [US5] W251: Extract `fn validate_one_hand(name: &str, prof: &Profession, label: &str, result: &mut ValidatedBuild) -> Option<String>` and call it twice, assigning to `set.main_hand` / `set.off_hand`. Target: `crates/optimizer/src/validation.rs`. Record evidence in ledger.md#w251.
- [ ] T255 [US5] W252: Delete the orphan `// 6. Target: `crates/optimizer/tests/live_llm.rs`. Record evidence in ledger.md#w252.
- [ ] T256 [US5] W253: Replace with what the file actually is, e.g. `//! Regression suite for combat-math fact interpretation; one test per permutation.` Target: `crates/optimizer/tests/math_permutations.rs`. Record evidence in ledger.md#w253.
- [ ] T257 [US5] W254: Inline the Tank-Chrono rationale (the following three lines already state it) and drop 'see NOTES in the report'; change 'Three' to 'Four'; at line 190 either switch `daredevil_wvw_roam` to the real WvW profile weights via `to_weights_for` and re-pin, or say why the PvE power preset is deliberately used. Target: `crates/optimizer/tests/scoring_regression.rs`. Record evidence in ledger.md#w254.
- [ ] T258 [US5] W255: Replace the trailing comment with `// Requires the in-game addon's live items cache (see dev.cfg.example)`. Target: `crates/optimizer/tests/upgrade_graph_live.rs`. Record evidence in ledger.md#w255.
- [ ] T259 [US5] W256: Add a short 'Second deployable: server/feedback' section (crate layout, endpoints, deploy path, pointer to server/feedback/deploy/README.md) and one line each for radio/news/themes in the crates/addon row. Target: `docs/architecture.md`. Record evidence in ledger.md#w256.
- [ ] T260 [US5] W257: Tick the completed steps and add a dated Status section (as the foundational-remediation plan has) naming Task 7 as the only outstanding item, or archive the plan and move the remaining cleanup into an issue. Target: `docs/superpowers/plans/2026-08-26-per-slot-gear-implementation.md`. Depends on W188. Record evidence in ledger.md#w257.
- [ ] T261 [US5] W258: Drop `"trace"` from tower-http's feature list (or add the TraceLayer the feature was meant for) and move `tower` to [dev-dependencies]. Target: `server/feedback/Cargo.toml`. Record evidence in ledger.md#w258.
- [ ] T262 [US5] W259: Review the documented deliberate limitation against current code; retain with rationale and a concrete reconsideration trigger if still valid. Target: `server/feedback/src/ratelimit.rs`. Record evidence in ledger.md#w259.
- [ ] T263 [US5] W260: Delete the two duplicated tests from tests/api.rs and keep the `#[cfg(test)] mod tests` copies next to the implementation. Target: `server/feedback/src/ratelimit.rs`. Record evidence in ledger.md#w260.
- [ ] T264 [US5] W261: Rename to what they assert, e.g. `admin_list_newest_first_and_leaves_rows_received` and `admin_get_one_returns_full_row_without_marking_read`. Target: `server/feedback/tests/api.rs`. Record evidence in ledger.md#w261.
- [ ] T265 [US5] W262: Use `unwrap_or_else(|e| e.into_inner())` for both statics, matching the pattern the rest of the crate documents. Target: `crates/addon/src/ui/icons.rs`. Record evidence in ledger.md#w262.
- [ ] T266 [US5] W263: Gate the module for its real consumers - `#[cfg(any(test, feature = "dev-config"))]` (examples/tests enable the feature) - and drop `cache_dir_or_exit`'s `process::exit` in favour of returning the `Result` that `cache_dir` already provides. Target: `crates/gw2api/src/dev_config.rs`. Record evidence in ledger.md#w263.
- [ ] T267 [US5] W264: Verify once in-game and either delete the comment (keeping or switching the separator) or, if the claim is true, fix the other sites and move the constraint into fonts.rs docs. Target: `crates/addon/src/ui/main_view/tabs/radio.rs`. Record evidence in ledger.md#w264.
- [ ] T268 [US5] W265: Either use real 0.35/0.25 multipliers for the injected rows (so the numbers are on the same scale as the rest of the output), or skip the forced-prefix block entirely when the prefix is not in the live cache and print why, rather than inventing a row. Target: `crates/optimizer/examples/nudge_druid_check.rs`. Record evidence in ledger.md#w265.
- [ ] T269 [US5] W266: Replace with the ScriptedServer pattern already used in openai_compat.rs tests: point ANTHROPIC_API_BASE at a loopback server, call validate_key/validate_key_detailed, and assert the recorded request is GET /models. Target: `crates/optimizer/src/llm/anthropic.rs`. Record evidence in ledger.md#w266.
- [ ] T270 [US5] W267: Move one stemmer into text_util (next to normalize_sigil_family) and use it from all four sites. Target: `crates/optimizer/src/search_v2.rs`. Record evidence in ledger.md#w267.

## Final phase: Campaign verification

- [ ] T271 Reconcile all 268 entries, duplicate closure and exclusions in specs/003-audit-remediation/ledger.md; no unexplained open finding.
- [ ] T272 Execute applicable workspace/server checks and document outcomes in specs/003-audit-remediation/verification.md.
- [ ] T273 Complete per-batch versioned DLL handoff and user in-game acceptance in specs/003-audit-remediation/verification.md before any commit/push/release.

## Dependencies and execution order

US1 → US2 → US3 → US4 → US5 → final reconciliation. The per-entry dependency graph overrides list order within each story: select an unblocked task, never close its dependent first. If a dependency lives in a later story, pull that prerequisite forward only with its consumer. Restore W001 during US1 so CI can verify every subsequent batch. W012 shares W011 persistence work. W044 precedes W013/W015/W016/W019. W032 precedes W026/W043. W030 precedes W027/W028/W029. Duplicate IDs close only when their canonical implementation and distinct acceptance are verified.

## Parallel execution examples

- US1: read-only storage and radio regression design in separate files; serialize actual shared-state edits.
- US2: independent patch-data reference analysis and Choya projection research.
- US3: renderer reference analysis and optimizer parser consumer analysis.
- US4: server manifest checks and documentation reference review.
- US5: independent current-code adjudication for unrelated contested claims.

No tasks are marked [P] for concurrent implementation while another agent is changing this shared tree. Coordinate ownership or recheck diffs before writing; run Cargo jobs sharing target output sequentially. Long commands use Terminal Commander.

## Incremental delivery

Deliver US1 as the first coherent verified batch, then the B001 stat correction, then the remaining stories. Update individual checkboxes only after their acceptance criteria pass. An in-game handoff is a checkpoint; it does not mark pending campaign items complete. The audit's deliberate no-action proposals require documented dispositions, not artificial changes.
